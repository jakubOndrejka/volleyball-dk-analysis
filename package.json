{
  "name": "volleyball-dk-analysis",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "scrape": "node scraper.js",
    "collect": "node collect-analysis.js",
    "analyze": "node build-analysis.js",
    "refresh": "npm run scrape && npm run collect && npm run analyze",
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.js",
    "build": "node scripts/build-site.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "pdfjs-dist": "^5.4.624"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
